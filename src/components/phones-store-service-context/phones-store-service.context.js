import React from "react";

const {
    Provider: PhonesStoreServiceProvider,
    Consumer: PhonesStoreServiceConsumer,
} = React.createContext();

export {
    PhonesStoreServiceProvider,
    PhonesStoreServiceConsumer,
}